# AGENTS.md

## Git rules (strict)

- **Never commit or push to `main` unless the user explicitly says so.**
- **When committing to a branch, always ask the user for the commit message first** before running `git commit`.
- Do not push any branch unless the user explicitly says so.
