{
  "name": "projectobts-mern",
  "version": "0.1.0",
  "private": true,
  "description": "OBTS MERN monorepo orchestrator (server + client)",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run build",
    "test": "npm --prefix server test && npm --prefix client run test",
    "build": "npm --prefix client run build",
    "check": "npm --prefix server run typecheck && npm --prefix server test && npm --prefix client run build && npm --prefix client run test"
  },
  "license": "ISC"
}